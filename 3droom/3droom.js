// select button and add event listener
document.addEventListener("click", () => {
  // add a class which toggles the div containing table
  document.querySelector(".table").classList.toggle("change");
});
