// select dots-btn and add event listener
document.querySelector(".dots-btn").addEventListener("click", () => {
  // add .change class to toggle the button
  document.querySelector(".container").classList.toggle("change");
});

